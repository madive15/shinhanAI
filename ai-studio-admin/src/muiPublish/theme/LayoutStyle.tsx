import { createTheme } from "@mui/material/styles";

const LayoutStyle = createTheme({
    components: {
        MuiPaper: {
            // root: {},
        },
    },
});
export default LayoutStyle;
